<h1>Productos</h1>
<section id='products'>
    {{#each products}}
    <article id={{id}}>
        <h2>{{title}}</h2>
        <h3>{{description}}</h3>
        <h3>${{price}}</h3>
        <h3>Stock: {{stock}}</h3>
        <hr>
    </article>
    {{/each}}
</section>