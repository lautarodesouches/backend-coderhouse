export default class Ticket {

    constructor(code, time, amount, purchaser) {
        this.code = code
        this.time = time
        this.amount = amount
        this.purchaser = purchaser
    }

}